import { ContainerLayout, AccessButton } from "components"

export const Login = () => {
    return (
        <ContainerLayout>
            <AccessButton text={"Log in"} />
        </ContainerLayout>
    )
}

