import { ContainerLayout, AuthLayer, LoginForm } from "components"

export const Login = () => {
    return (
        <ContainerLayout>
            <AuthLayer />
            <LoginForm />
        </ContainerLayout>
    )
}

