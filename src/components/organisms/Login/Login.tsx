import { LoginLayout, AuthLayer, LoginForm } from "components"

export const Login = () => {
    return (
        <LoginLayout>
            <AuthLayer />
            <LoginForm />
        </LoginLayout>
    )
}

